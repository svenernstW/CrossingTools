# Use C++17
CXX_STD = CXX17

# Ask R for the right OpenMP flags for this platform (may be empty if unsupported)
PKG_CXXFLAGS += $(SHLIB_OPENMP_CXXFLAGS)

# Link to OpenMP and BLAS/LAPACK that R was built with
PKG_LIBS += $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
