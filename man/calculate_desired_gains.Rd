% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_desired.R
\name{calculate_desired_gains}
\alias{calculate_desired_gains}
\title{Calculation of desired gains index (Werner et al.)}
\usage{
calculate_desired_gains(
  A,
  V,
  V.approx,
  gains = NULL,
  use.V = TRUE,
  use.marginal.V = FALSE,
  use.V.approx = FALSE,
  nThreads = 4L
)
}
\arguments{
\item{A}{A matrix (n_genotype x n_trait) of multi-trait BLUPs.}

\item{V}{Numeric matrix ((n_genotype\emph{n_trait) x (n_genotype}n_trait)) with the
posterior variance var(\eqn{\tilde{g}}).
Required if \code{use.V} or \code{use.marginal.V} is TRUE.}

\item{V.approx}{Numeric matrix (n_trait x n_trait) giving an approximation of
var(\eqn{\tilde{g}}), e.g. \code{cov(A)}. Required if \code{use.V.approx} is TRUE.}

\item{gains}{Numeric vector (length = n_trait) of desired gains. If NULL, uses
equal weights (\code{rep(1, n_trait)}).}

\item{use.V}{Logical. If TRUE, uses each genotype's own trait-block \code{V_g}
to compute a per-genotype index (i.e., \eqn{w_g = V_g^{-1} d}).}

\item{use.marginal.V}{Logical. If TRUE, uses the \emph{marginal} posterior variance
(mean of per-genotype trait-blocks from \code{V}).}

\item{use.V.approx}{Logical. If TRUE, uses \code{V.approx}.}

\item{nThreads}{Number of OpenMP threads.}
}
\value{
A one-column data.frame with the desired-gains index for each genotype.
}
\description{
Calculates the desired gains index based on multi-trait BLUPs and a chosen
(co)variance matrix option.
}
\examples{
\dontrun{
set.seed(1)
nG <- 50; nT <- 3
A <- matrix(rnorm(nG * nT), nG, nT)               # BLUPs
V.approx <- cov(A)                                 # simple approximation
V <- kronecker(diag(nG), V.approx)                 # block-diagonal (nG*nT x nG*nT)
gains <- c(1, 0.5, 0.2)

# Using the approximation:
idx1 <- calculate_desired_gains(A, V, V.approx, gains,
                                use.V = FALSE, use.marginal.V = FALSE,
                                use.V.approx = TRUE)

# Using the marginal V (averaged across genotype blocks):
idx2 <- calculate_desired_gains(A, V, V.approx, gains,
                                use.V = FALSE, use.marginal.V = TRUE,
                                use.V.approx = FALSE)

# Using per-genotype V blocks:
idx3 <- calculate_desired_gains(A, V, V.approx, gains,
                                use.V = TRUE, use.marginal.V = FALSE,
                                use.V.approx = FALSE)
}
}
