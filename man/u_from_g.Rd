% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/u_from_g.R
\name{u_from_g}
\alias{u_from_g}
\title{Backsolve marker effects (mu) from individual effects (g)}
\usage{
u_from_g(
  M,
  G,
  g,
  scaling.factor,
  tol = 1e-10,
  LD.var = FALSE,
  grouping = NULL,
  calc.prior.Vcov = FALSE,
  prior.Vcov = NULL,
  sigma.sq = NULL,
  calc.posterior.Vcov = FALSE,
  PEV = NULL,
  n.Threads = 4L
)
}
\arguments{
\item{M}{Numeric matrix (n x p): marker/genotype matrix (individuals in rows, markers in columns).}

\item{G}{Numeric matrix (n x n): relationship matrix among individuals.}

\item{g}{Numeric (n x k) or length-n vector: individual effects (one or more traits).}

\item{scaling.factor}{Numeric scalar, that is used to scale the GRM.}

\item{tol}{Numeric scalar tolerance for near-singularity checks in \code{G}.}

\item{LD.var}{Logical; if \code{TRUE}, compute LD variance by groups.}

\item{grouping}{\code{NULL} or a data.frame with a column \code{chr} of length \code{ncol(M)}
giving group membership (only used if \code{LD.var=TRUE}). If \code{NULL} with \code{LD.var=TRUE},
all markers are treated as one group. If supplied be aware that the order must match exaclty that of M}

\item{calc.prior.Vcov}{Logical; if \code{TRUE}, compute prior Vcov over marker-by-trait effects.}

\item{prior.Vcov}{\code{NULL}, \code{p x p} or \code{(p*k) x (p*k)} numeric matrix.}

\item{sigma.sq}{\code{NULL}, scalar, length-k vector, or \code{k x k} matrix specifying trait co-variance(s);
required if \code{calc.prior.Vcov=TRUE}.}

\item{calc.posterior.Vcov}{Logical; if \code{TRUE}, compute posterior Vcov using \code{PEV}.}

\item{PEV}{\code{NULL} or numeric matrix of size \code{(n*k) x (n*k)} (prediction error variance).}

\item{n.Threads}{Integer larger 1; OpenMP threads (if enabled at compile time).}
}
\value{
A list with elements matching the C++ return:
\describe{
\item{\code{mu_matrix}}{\code{p x k} matrix of marker effects.}
\item{\code{variance_LD}}{LD variance by groups (matrix if \code{k=1}, else list), or \code{NULL} if \code{LD.var=FALSE}.}
\item{\code{prior_Vcov}}{Prior Vcov matrix if requested, else \code{NULL}.}
\item{\code{posterior_Vcov}}{Posterior Vcov matrix if requested, else empty \code{0x0} matrix.}
\item{\code{group_levels}}{Group labels used (only when \code{LD.var=TRUE}).}
}
}
\description{
Computes marker effects \eqn{\mu = (scalingFactor * M' * G^{-1}) * g}, with
optional prior/posterior variance-covariance outputs and LD-variance by groups.
}
