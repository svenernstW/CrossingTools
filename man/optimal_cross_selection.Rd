% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimal_cross_selection.R
\name{optimal_cross_selection}
\alias{optimal_cross_selection}
\title{Optimal cross selection via a genetic algorithm (with fixed crosses)}
\usage{
optimal_cross_selection(
  crosses,
  fixed.crosses = NULL,
  ncrosses,
  target.angle,
  u,
  u.fixed = NULL,
  G,
  propability.mutate = 0.01,
  nmutate = 2,
  nselect = 500,
  npop = 10000,
  max.generation = 100,
  max.iteration = 100,
  angle.penalty = 0.5,
  nthreads = 4L
)
}
\arguments{
\item{crosses}{integer matrix (K x 2): candidate variable crosses (1-based indices)}

\item{fixed.crosses}{integer matrix (F x 2): crosses that must be in the solution (can be 0 rows)}

\item{ncrosses}{integer: total crosses in final plan (variable + fixed)}

\item{target.angle}{numeric (radians): target tradeoff angle}

\item{u}{numeric length K: utility for each row of \code{crosses}}

\item{u.fixed}{numeric length F: utility for each row of \code{fixed.crosses} (can be length 0)}

\item{G}{numeric square GRM (nInd x nInd)}

\item{propability.mutate}{numeric in \link{0,1}}

\item{nmutate, }{nselect, npop, max.generation, max.iteration integers}

\item{angle.penalty}{numeric >= 0}

\item{nthreads}{integer >= 1}
}
\value{
list from cpp_optimal_cross_selection()
}
\description{
Optimal cross selection via a genetic algorithm (with fixed crosses)
}
