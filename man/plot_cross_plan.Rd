% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cross_plan.R
\name{plot_cross_plan}
\alias{plot_cross_plan}
\title{Plot predicted progeny distributions for a crossing plan}
\usage{
plot_cross_plan(cross.plan, cross.df, traits = NULL, nsamples = 1000L)
}
\arguments{
\item{cross.plan}{Data frame, matrix, or object coercible to a data frame
containing the crossing plan. It must have two columns for biparental
crosses or four columns for four-parent crosses. Rows determine the
displayed cross labels and order.}

\item{cross.df}{Data frame, matrix, or object coercible to a data frame
containing cross-specific predictions, typically produced by functions
for calculating segregation variance, cross expectation, SPV, or OHV.
Trait-specific columns must follow the naming convention
\code{<metric>.<trait>}, for example \code{GEBV.Yield},
\code{SPV.Yield}, \code{var.A.Yield}, or \code{var.TGV.Yield}.
Biparental crosses are matched using columns \code{parent1} and
\code{parent2}, \code{male} and \code{female}, or the first two columns.
Four-parent crosses require columns \code{parent1} to \code{parent4}.}

\item{traits}{Character vector specifying the trait names to plot, or
\code{NULL} to plot all traits detected in \code{cross.df}.}

\item{nsamples}{Positive integer. Number of random samples drawn per cross,
trait, and distribution type to visualise the predicted distributions.
The default is 1000.}
}
\value{
A \code{ggplot} object containing trait-specific predicted progeny
distributions and available cross-criterion values.
}
\description{
Visualises the predicted progeny distributions and available cross criteria
for the crosses in a crossing plan. Separate panels are produced for each
trait.
}
\details{
Predicted progeny distributions are represented by normal distributions
defined by the expected cross value and the corresponding segregation
variance. The displayed densities are generated by random sampling from
normal distributions with the predicted means and variances for
visualisation only; they do not represent simulated progeny or meioses.

For additive genetic values, the progeny distribution is defined by the
expected genomic breeding value (GEBV) and additive segregation variance.
When dominance is included, the total genetic distribution is defined by
the expected total genetic value (TGV) and total genetic segregation
variance, including additive variance, dominance variance, and the
additive--dominance covariance contribution.

Available cross criteria are plotted on the distributions as points. These
can include the expected genomic breeding value (GEBV), expected total
genetic value (TGV), superior progeny value (SPV), total superior progeny
value (TSPV), and optimal haploid value (OHV). Only criteria present in
\code{cross.df} are displayed.
}
